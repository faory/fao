#include <iostream>
#include <fstream>
#include <string>

int main()
{
    std::string mot { "" };
    if (std::ifstream lectureFichier {"/fichiers/diskotek"})
    {
        lectureFichier >> mot;
    }
    else
    {
        std::cout << "Le fichier est introuvable !\n";
    }

    std::cout << mot ;
}